# 数据库配置
news:
  aggregation:
    mysql:
      url: jdbc:mysql://192.168.1.7:3306/news-aggregation
      username: root
      password: wangchuan
  rss:
    sources:
      - name: BBC Business
        url: https://feeds.bbci.co.uk/news/business/rss.xml
        category: Business

# 导入 datasource 配置
spring:
  config:
    import: classpath:datasource.yml
  autoconfigure:
    exclude: org.apache.shardingsphere.spring.boot.ShardingSphereAutoConfiguration

# MyBatis 配置
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
